# Universal GVP — Organization-Wide Values, Principles, Heuristics, Rules
#
# Root of the inheritance chain. No parent. All other scopes inherit
# from this (directly or transitively). See schema.yaml for field
# definitions and inheritance rules.
#
# For personal use, this file can remain empty. For organizational use,
# it captures items that apply across all teams and projects.

meta:
  name: universal
  scope: universal
  id_prefix: "U"

values: []

principles: []

heuristics: []

rules: []
